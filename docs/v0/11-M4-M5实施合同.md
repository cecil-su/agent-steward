# M4 / M5 实施合同

状态：已实现并通过本轮 macOS 验收。本文收敛 `07-实施路线图` 的后续阶段，优先于早期 API 草案；不计入 V1。交付状态见本文末尾。

## M4：显式绑定与可观察事件

### 身份与生命周期

- `session bind <session-id> --source <source> --external-session <id> --if-version <v>`：给已存在且未结束、Task 未关闭的本地 Session 一次性绑定来源。已有同值绑定是当前版本 no-op；不同绑定拒绝。`source + externalSessionId` 全库唯一。绑定写 Task version 和 History；不改变当前 Session。
- `claim/resume` 保持原合同。客户端的 started/resumed/idle/closed 是观测事件，绝不映射为 claim/resume/session close。失联和迟到事件不能回收执行权。
- 每次事件必须携带明确本地 Session ID、来源和外部 ID，与绑定完全匹配；不根据 cwd、最新 Task 或当前窗口猜测归属。客户端同一会话不能自动跨 Task 改绑。

### 接收合同

`hook ingest --input <file>` / `--json --input -` 接收一个严格 JSON：

```json
{"schemaVersion":1,"sessionId":"session-a","source":"generic","externalSessionId":"client-a","eventId":"event-1","kind":"idle","occurredAt":"2026-09-07T00:00:00Z"}
```

- `kind` 为 started/resumed/idle/closed/user_message/assistant_message/tool_call/tool_result/error。消息和工具事件首期只记录种类和时间，不保存正文、工具参数、结果、自由格式错误或附件；这些字段在适配器投影时丢弃，严格接收接口拒绝未知字段。保留身份字段只允许有界 ASCII 标识符。用户仍可用已审查的手工 Import 保存内容。
- 自动采集默认不启用；用户显式配置 Hook 和绑定后才启用。不得扫描客户端记录目录。客户端适配器必须从官方实际能力取事件，不能声称不支持的生命周期已接入。
- 单事件输入上限 16 KiB；日期必须为带时区 RFC3339，标准化为 UTC。标识符 1–128 字节，source 1–32 字节；禁止空白、路径、URL 和任意文本字段。不得将未知原文、输入全文或凭据写入错误、日志、Task、History 或临时重试文件。
- `(sessionId, eventId)` 唯一；相同标准化事件重放返回原记录，不新增；同键不同内容返回 `HOOK_EVENT_CONFLICT`。外部绑定先校验，不会因 ID 碰撞接受另一个来源。
- 事件独立于 Task mutation：追加不改变 Task、Session 的执行生命周期、Task version、updatedAt 或 History。已结束 Session 可接收迟到事件；观测日志不表示执行权。
- SQLite 事务保证校验、去重、插入和数量上限原子完成。每 Session 最多 10,000 条（含删除后的去重标记），满时返回 `HOOK_CAPACITY_REACHED`，不驱逐旧去重键。不提供无限队列或后台补偿。
- Hook 失败给出稳定错误并快速返回，不影响显式 CLI。自动包装器最多在短暂 busy 时重试两次；无落盘队列、无后台扫描；失败退出码和 stderr 让宿主显示丢失风险。接收失败不能被报告为已采集。
- `hook list <session-id> --after <sequence> --limit <1..200>` 按数据库 sequence 分页。展示发生时间和接收时间，乱序事件不重排本地执行状态。
- `hook clear <session-id> --if-version <v> --yes` 显式删除可见内容并留下最小去重标记，防止已删除事件被重试复活；递增 Task version，History 只保存 Session ID 和删除计数。使用 secure_delete 和 WAL checkpoint，保留既有物理擦除限制。

### 存储

增加 `session_events`，外键指向既有 Session，不建设通用 Event/Actor/Operation 模型。新库 schema 标识递增，旧库拒绝打开，不做隐式迁移或重建。JSON envelope 仍为 2；事件 DTO 自身 schemaVersion 为 1。

## M5：本地服务与任务界面

- 单个 Rust Daemon 从启动参数固定数据库，提供同源静态 GUI 和 JSON API。默认绑定 `127.0.0.1`，默认随机空闲端口；用户可显式通过 `--bind <本机IPv4>` 绑定指定网卡地址，不允许通配、多播或广播地址，不接受请求切换数据库。Host/Origin 严格匹配实际监听地址和端口，不放宽认证或 CORS。非回环 HTTP 不加密传输，启动时告警，使用者须限制网络访问；程序不自动修改防火墙。
- 所有业务经过 Application Service；SQLite/Git 同步工作在线程池执行，有界并发，不能阻塞 HTTP 事件循环。保留 CAS、工作树锁、dirty 检查、部分完成恢复合同。
- 首次启动创建管理员与只读两份随机凭据，写入用户专属 runtime/identity 目录并跨重启保留，普通 stdout/stderr 仅显示 URL 和凭据文件路径，不显示秘密。默认启动时通过本机浏览器启动器传递随机一次性连接码（120 秒有效），页面立即清除 URL fragment，再通过 `POST /api/connect` 兑换凭据；码只能成功使用一次，兑换遵循同样的 Host/Origin 边界，不按来源 IP 放行。`--no-open` 禁止自动打开；其他设备使用只读凭据手动连接，自动连接失败时本机可手动使用管理员凭据；权限由凭据而非来源 IP 决定。凭据保存在页面内存及当前标签页 sessionStorage；刷新后自动认证恢复，主动断开或收到 401 时清除，Daemon 重启后持久凭据仍有效，一次性连接码失效；撤销需停止服务后明确移除对应凭据文件并重启。关闭标签页通常清除会话存储，但浏览器恢复会话可能恢复该存储；需要确定清除时应主动断开。无 Cookie、无 localStorage；长期凭据不进入 URL，一次性连接码仅放入本机启动链接的 fragment、不进入查询参数或日志。无日志正文。
- 每个 API 请求都检查精确 Host，存在 Origin 时必须匹配；拒绝 null/跨 Origin 和跨站 Fetch Metadata。API 要求认证请求头，POST 还要求 JSON Content-Type。无 CORS 放行；静态资源禁止跨源嵌入，CSP 不允许内联脚本和第三方资源；用户内容仅作为文本渲染。
- API/CLI 共用数据库权限告警。GUI 显示 warnings 和结构化错误，认证失败回到连接界面。
- 请求体总上限 1 MiB（Hook 为 16 KiB）；路径参数使用明确绝对路径，不能使用 Daemon cwd 推断浏览器目录。文件导入需要显式已审查确认，拒绝非普通文件和超限输入；不提供任意文件读取端点。
- JSON 成功和失败都使用 schemaVersion=2 envelope。400 输入无效、401 认证失败、403 来源拒绝、404 不存在、409 CAS/Session/Hook 冲突、503 busy、500 其它失败；不得依赖自然语言解析错误。
- 所有写操作只在用户提交时发送一次。禁止自动重试 create/Worktree 等写请求；超时或断连提示“结果未确认，请刷新核对”。冲突保留用户输入并显示新版本，用户重新审查后提交；不能自动把最新 version 填回旧 Patch 重放。
- GUI 提供任务列表（固定视图、搜索、分页）、详情编辑、备注/阻塞/解除阻塞/明确关闭、Checkpoint、Session 历史、Hook 观测、Import 元数据、History、实时 Worktree 状态与上下文复制。Session 恢复/领取和 Worktree 写入必须显式提交，接管和删除展示具体目标及确认。不会根据测试或摘要自动关闭任务。
- 提供管理员/只读两种凭据；后端在读取写请求体和执行业务操作前拒绝只读凭据的非 GET/HEAD 请求。通过认证 SSE 推送数据失效通知，页面重新查询快照；每秒观察 SQLite data_version，支持 CLI/Hook 外部提交。最多 16 条独立订阅，断线重连但不重放写请求，表单打开时只提示待刷新。保留手动刷新，不引入 WebSocket 或通用 Operation 模型。API 与 CLI 复用结果，GUI 不解析终端文本。

## 验收与执行记录

- [x] 显式绑定：正常、同值 no-op、跨 Task/重复外部 ID、已结束和旧版本拒绝。
- [x] 事件：重复、冲突、乱序/迟到、未知字段、大小上限、分页、容量、清除后重放不复活；Task 不被观测事件修改。
- [x] 通用 JSON 适配：明确协议、元数据投影、配置示例、失败回退；补充 Codex Hooks 与 pi 扩展原生入口，见 [适配指南](../../integrations/README.md)。
- [x] Daemon：认证、Host/Origin、CSRF、请求限制、凭据文件权限、权限告警；管理员/只读权限及持久凭据重启复用，连接码一次性消费。
- [x] HTTP：业务错误 envelope、CAS、多入口并发、未知结果不自动重试、Worktree 部分完成。
- [x] GUI：浏览器端创建/查看/编辑、冲突保留输入、Session/Checkpoint/History、上下文复制和退出认证。
- [x] 全量 Rust 测试、Clippy、格式、diff 检查；记录验证平台和未验证项。

任务连续性数据库：仓库 `.local/work.db`，任务 `V0-M4-M5`；该目录不提交。实现数据库升级期间用 `.local/taskctl-tracker`（原构建副本）保存 Checkpoint，避免修改任务数据库 schema。


### 本轮验收证据（2026-09-07，macOS）

- `cargo test --workspace --locked`：77 项测试通过；覆盖 M1–M3 回归、M4 绑定/观测、M5 HTTP/凭据与共用权限检查。
- `cargo clippy --workspace --all-targets --locked -- -D warnings`、`cargo fmt --all -- --check`、`git diff --check`：通过。
- 真实 Chrome + 临时 Daemon：创建、领取、Checkpoint、CAS 冲突保留输入、恢复 Session、绑定/清除 Hook、Import 删除、阻塞/解除、响应丢失不重放、Worktree dirty 删除保护、上下文复制、显式关闭、移动端布局和当时的内存认证：通过。后续 sessionStorage 连接恢复改动需单独验证。
- 最终 review 补齐 `task notes` / HTTP notes 查询和 GUI 展示，防止备注写入后无法查看；修复提交成功但刷新失败的提示、可选关闭原因和异步列表竞态。
- Windows/Linux、真实客户端模型运行未验证；正文、附件、model/usage、实时订阅属于明确延后项。当前交付为通用及 Codex/pi 元数据 Hook 与本地工作台，不扩大为完整聊天归档系统。
- 未自动关闭 Task，未提交或推送。使用方法和测试重现见 [使用与验收](12-M4-M5使用与验收.md)。
