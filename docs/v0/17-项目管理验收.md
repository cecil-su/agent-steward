# #34 项目管理——人工验收交接

> **人工状态：未执行，待确认人工验收实例。** 当前Windows自动化已包含真实Chrome完整流程（见运行记录），不等于人工业务验收、Linux验证、迁移或发布完成。

## 0. 现场与范围

- 任务：#34 `0907｜优化｜Agent Steward 项目事实复用与任务上下文注入`；本批按用户确认聚焦项目功能，缓存与 Pi 桥接暂缓。
- 仓库：`E:/ai/agent-steward`，分支 `codex/v0-taskctl`。本批核对 HEAD 为 `1783c5c`，工作区已吸收远程至 `8e91fd0`，仍未提交/前移 HEAD。**测试前必须再次记录完整 HEAD、dirty 差异及所用 taskctl/taskd 的 SHA-256，不能只用该 HEAD 认定版本一致。**
- 源码入口：`crates/server/src/projects.rs`、`crates/server/src/lib.rs`、`crates/server/web/{index.html,app.js}`、`crates/application/src/{projects,sources,project_context}.rs`。
- 完整合同：[第16篇](16-项目与上下文复用.md)。版本/权限/部署边界：[第12篇](12-M4-M5使用与验收.md)、[第14篇](14-UI独立发布.md)。
- 资料核对日期：2026-09-08（Asia/Shanghai）；自动化执行范围/结果以本批 #34 Checkpoint 为准，人工执行时重新锁定现场。
- 目标：当前开发 Schema 4 的**全新隔离库**。正式 Schema 2 库不在本次人工操作范围；没有隐式升级。当前正式环境未部署本批代码。
- 用户指定的测试地址为 `http://172.19.10.185`；已确认属于本机，并使用新隔离实例通过真实浏览器回归，测试后已退出。**不是当前常驻人工验收地址**；人工实例/角色/预置数据仍待确认。此前127.0.0.1临时地址受安全策略拦截，保留为历史证据；本批未改变安全策略或构造正式数据。

## 1. 判定与写风险

- PASS：前置、步骤和预期均满足，留存对应版本/环境证据。
- FAIL：前置已满足，但产品结果偏离预期。
- BLOCKED：缺目标版本、URL、角色、浏览器、测试数据或平台；不能据此宣称产品失败或通过。
- 查询页面/上下文为只读；项目创建/改名/组件/源码登记与任务关联会写**测试任务库**。解除源码登记不删除目录，解除任务项目不删除项目，但跨项目/清除会清空组件范围。
- 只在批准的临时库/临时源码上执行。正式库、真实 Mailroom 源码及服务禁止写入。停止/删除测试环境也须确认目标，不能清理正式文件。
- DevTools 只观察页面自然请求；不手工篡改、重放写请求或修改凭据。未知字段/权限绕过等使用自动化合同证据，另行授权后才做专用 API 攻击测试。

## 2. 入口与权限速查

| 系统入口 | 实际地址 | 源码路径/主要请求 | 权限 |
| --- | --- | --- | --- |
| 工作台顶部“项目管理” | `TEST_URL`，待确认 | 页面 `/` 内的项目区域；GET `/api/projects` | admin / reader |
| 项目详情 | 同上 | GET `/api/projects/{ref}`、`/components`、`/sources` | admin / reader |
| 创建/改名/源码登记 | 同上 | POST `/api/commands/project-*`，项目 expectedRevision | admin |
| 任务详情“关联项目”“设置组件范围” | 同上 | POST `/api/commands/task-project`、`task-components`，Task expectedVersion | admin |
| 任务列表“筛选项目” | 同上 | GET `/api/tasks?project=...` | admin / reader |
| 项目详情“查看源码上下文” | 同上 | GET `/api/projects/{ref}/context?sourceId=...&worktree=...` | admin / reader |
| CLI | 经确认的开发二进制 | 每次显式 `--database TEST_DB` | 测试机当前用户 |

所有源码/工作区路径均属于**服务所在机器**，不是浏览器客户端路径，不上传文件。`##ID` 表示 Project，`#ID` 表示 Task；URL 参数须编码 `#`。

旧后端的 `/api/access` 没有 `projectManagement:true` 时，新 UI 隐藏项目入口及相关输入；这属于目标版本未就绪，应先判 BLOCKED。该声明不授予写权限。只读验收必须使用批准的严格认证测试实例（`--require-local-auth`）；默认本机免登录模式会信任本机调用，不适合验证 reader。

## 3. 前置与数据准备单

- [ ] 环境负责人确认仅启动新的隔离 taskd，不重启/替换正式服务。使用临时端口、显式新 TEST_DB、独立 runtime、`--no-open`；需要 reader 验证时启用严格认证。
- [ ] 确认使用本批编译结果而非全局旧 taskctl/taskd；UI与API能力一致，无旧外置UI覆盖。
- [ ] 填写实际 URL、OS、浏览器及版本、二进制hash、测试库与源码目录。凭据只通过已有登录流程使用，不写入本文或截图。
- [ ] 管理员批准测试库写入，并准备下列数据；不得用未经确认的业务数据代替。

| 代号 | 构造约束/需提供内容 | 初始预期 |
| --- | --- | --- |
| P-A / P-B | 两个批准的唯一测试项目名称，不以#开头、非纯数字 | 均未创建；创建后记录实际项目ID |
| SRC-G | 临时Git checkout绝对路径；内部有README.md及源码子目录；记录HEAD/status和文件hash | 无业务执行；不使用真实业务仓库 |
| SRC-D | 临时非Git目录绝对路径，内有测试README.md；记录hash | 目录及祖先不带Git标记 |
| C-A | P-A内的组件名称，例如经批准的测试组件代号 | 尚未创建 |
| T-A / T-B | 隔离库中的测试任务；标题遵守MMDD｜类型｜主题 | 未开始、未关联项目/组件/Session/Worktree；记录实际Task ID |
| PAGE-A | 可选分页数据：至少51个测试项目、P-A至少31个测试任务 | 由负责人批准后预置，缺失则分页项BLOCKED |
| ADMIN-A / READER-A | 同一隔离实例的管理员、只读浏览器授权 | 只记录角色，不记录凭据或Cookie |

## 4. 基线与主用例

以下“原基线”是需求背景，不是当前环境实测异常。

| 用例 | 原基线 | 正确预期 |
| --- | --- | --- |
| TC34-P1 | 项目只有CLI基础，无管理页面 | 可独立创建/改名项目，不必先登记源码 |
| TC34-P2 | 页面无法登记项目源码 | 显式登记已有目录，文件及Git不变 |
| TC34-P3 | 页面无法设置任务项目/组件 | 仅改变任务归属，保留执行状态/Session/Worktree |
| TC34-P4 | 页面缺项目筛选和项目上下文展示 | 按稳定项目ID筛选，Task与源码上下文入口可用 |
| TC34-P5 | Project CAS未接入页面表单 | 冲突保留输入，使用Project revision而非Task version |
| TC34-P6 | 新页面权限/兼容尚需实际验证 | reader只读、旧后端不暴露不支持的操作 |

### TC34-P1 创建与改名（测试库写入）

前置：ADMIN-A、P-A/P-B，目标环境与版本已确认。

1. 全新打开 TEST_URL 并按批准方式连接，点击顶部“项目管理”。
2. 点击“新建项目”，输入P-A实际名称，确认提交；记录返回的 `##ID` 和 revision。
3. 不登记任何源码，确认项目详情提示暂无源码，仍有“查看该项目任务”入口。
4. 创建P-B；尝试以P-A同名（含ASCII大小写等价形式）再创建，确认拒绝且列表无重复。
5. 选择P-A，“修改项目名称”为批准的新名称；刷新项目列表和详情。

预期：ID不变，成功改名revision递增；不创建任务、Session、Worktree。项目名称按文本展示，不作为HTML执行。证据：前后项目行、ID/revision、自然POST状态/error.code。

### TC34-P2 组件与源码登记（测试库写入，不改源码）

前置：ADMIN-A、P-A、SRC-G/SRC-D及文件基线hash。

1. 从TEST_URL进入“项目管理”，选择P-A，点击“添加组件”并创建C-A。
2. “登记Git源码”：填SRC-G绝对工作区路径、目标源码相对路径及C-A，确认提交。
3. “登记普通目录”：填SRC-D绝对路径，选择“项目公共源码”，确认提交。
4. 刷新，确认两条源码分别显示Git Repository/相对路径、普通目录路径及正确组件。
5. 对SRC-D点击“解除源码登记”，勾选只解除元数据确认并提交。

预期：每次有效项目mutation只推进Project revision；源码ID按返回记录，不假定为1。SRC-D仍存在且README字节不变，SRC-G HEAD/status与文件hash无注册导致的变化；不创建/复制Git仓库。错误绝对路径/不匹配来源须明确拒绝。证据：项目修订、源码列表、前后文件hash与Git只读观察。

### TC34-P3 关联任务与组件（测试库写入）

前置：ADMIN-A、P-A/C-A、T-A/T-B。若尚无任务，先清除项目筛选，在“新建任务”中明确不填项目创建专用测试任务。

1. 新开TEST_URL，选择T-A，记录Task version/状态/当前Session/Worktree。
2. “关联项目”输入P-A的 `##ID`，阅读清空组件提示、勾选确认并提交。
3. “设置组件范围”，填C-A名称（每行一项），确认替换。
4. 将T-B关联P-B。再次读取T-A，确认未串项目。
5. 在经批准的T-B上，留空项目引用并确认解除；对照变更前基线。

预期：实际变更推进对应Task version，Project revision不因此改变；T-A/T-B状态、Session、Worktree不变。清除/跨项目时组件清空，同项目重复关联不清空组件。字段缺失或未勾确认不能意外清除。证据：任务ID、projectId/componentIds及前后版本/执行字段，不记录会话正文。

### TC34-P4 项目查询与上下文（只读）

前置：P-A/P-B与关联任务已完成，SRC-G仍登记；ADMIN-A或READER-A。

1. 新开TEST_URL，在项目管理中选择P-A，点击“查看该项目任务”。
2. 确认筛选框规范为 `##实际ID`，当前列表仅含P-A任务；切换任务状态视图/搜索，项目条件仍随请求传递。
3. 清除筛选，确认恢复所有项目任务范围，而不是解除任务归属。
4. 打开T-A，查看“所属项目”“组件范围”，点击“复制交接上下文”；核对项目、Task版本和最近Notes/截断提示。
5. 回到P-A项目页，“查看源码上下文”：显式填写本次SRC-G工作区路径并查询。普通目录不要求Git路径。

预期：源码导航展示项目/Source ID、当前目录、Git观察与文件路径；省略条目有提示，不展示源码正文。查询不改变任何Task/Project版本或History，不领取任务、不登记Worktree。缺失/错误Git工作区明确失败，不使用上次路径自动兜底。证据：自然GET过滤参数、页面/复制文本（脱敏）、前后版本；只读查询PASS不等于源码语义或业务验收通过。

### TC34-P5 冲突、失败与分页（测试库写入/只读组合）

前置：ADMIN-A的两个窗口、P-A；分页子项另需PAGE-A。

1. 两窗口均选P-A。窗口A打开“修改项目名称”并填写草稿，但不提交。
2. 窗口B成功修改P-A；A提交旧revision，确认冲突且草稿仍在。
3. A审查最新Project内容，点击“采用此版本，重新审查后提交”，自行确认草稿后再提交。
4. 对Task关联同样验证独立Task version；不要把Project revision当Task version。
5. 项目列表加载第二页后由另一窗口增加项目；刷新/自然SSE后已加载范围不退回第一页。P-A任务列表加载更多后仍携带项目条件。
6. 在390px窄视口检查项目/任务列表、表单、长路径和只读导航无横向溢出。

预期：冲突没有静默覆盖；表单打开时SSE不丢输入；迟到的组件查询不覆盖后来打开的编辑表单；切换项目先清空旧行/分页，请求失败后恢复不拼入旧项目行；无重复行或跨项目游标。写响应丢失、迟到刷新不关闭新表单等难以人工安全构造的场景引用自动化证据，不在共享环境拦截/重放请求。证据：两端revision/草稿、页行ID/nextAfter或nextCursor、截图及控制台错误摘要。

### TC34-P6 权限与兼容（主要只读）

前置：严格认证隔离实例的READER-A；旧后端兼容另需批准的旧版本测试实例，不能改正式服务。

1. 全新独立浏览器上下文以READER-A连接，进入项目管理并查询任务/源码导航。
2. 确认创建、改名、组件、源码写入、任务关联按钮不可用/隐藏；只读查询仍可用。
3. 通过已有自动化合同证据核对reader写请求在解析前被拒绝；人工不篡改或重放请求。
4. 在批准的旧后端测试实例检查缺少projectManagement能力时项目入口隐藏，任务原功能可用；缺环境则该子项BLOCKED。

预期：能力声明不授予写权限；不读浏览器存储中的长期凭据，不通过部署新版UI绕过旧数据库/后端限制。证据只保留角色、入口、状态码，不含Cookie/Token。

## 5. C-01 完整闭环与CLI对照

按 P1 → P2 → P3 → P4 顺序，用同一P-A/SRC-G/T-A记录全部实际ID与版本，再执行P5/P6。应能在新窗口重新定位项目、找到任务并读取项目/源码上下文；自动化或人工通过不自动关闭Task。

CLI对照在另一个批准的全新隔离库重新执行，P-A/T-A/Source ID须重新取得，不复用UI库的ID。以下占位符必须替换；所有引用加引号，所有命令显式指定测试库：

```text
<开发taskctl> --database <TEST_DB绝对路径> --json project create --name <P-A名称>
<开发taskctl> --database <TEST_DB绝对路径> --json project source add "##<P-A-ID>" --repo <SRC-G绝对路径> --path . --if-revision <最新revision>
<开发taskctl> --database <TEST_DB绝对路径> --json task create
<开发taskctl> --database <TEST_DB绝对路径> --json task project "#<T-A-ID>" --project "##<P-A-ID>" --if-version <最新Task版本>
<开发taskctl> --database <TEST_DB绝对路径> --json task list --project "##<P-A-ID>" --fields id,title,projectId
<开发taskctl> --database <TEST_DB绝对路径> --json task context "#<T-A-ID>"
<开发taskctl> --database <TEST_DB绝对路径> --json project context "##<P-A-ID>" --source <实际Source-ID> --worktree <SRC-G绝对路径>
```

先用project show/task show取得CAS，不能复用文档中的旧版本。CLI成功应exit 0且envelope.ok=true；失败应非零、结构化error且没有部分业务更新。Git/数据写入范围仍限于批准的fixture。

自动化来源：`crates/cli/tests/projects.rs` 的Git完整旅程、`crates/server/tests/projects.rs` 的HTTP流程/Project CAS/reader/路径校验、`crates/server/tests/browser/projects.test.cjs` 的DOM/VM页面流程及失败保护。实际执行命令/结果绑定本次Task检查点与工作区，不从测试文件存在推断已通过。

真实浏览器自动化已准备：`crates/server/tests/browser/smoke.cjs` 调用 `project-smoke.cjs`，覆盖项目创建/重名/CAS、组件、Git及公共目录源码、任务归属/组件/筛选、源码导航、reader与桌面/390px布局；使用原 smoke 的临时库、严格认证和临时Git数据。`CARGO_TARGET_DIR` 决定开发二进制路径，缺失时预检失败，不回退全局安装。已按用户指定本机地址执行完整真实浏览器用例，结果见下方；自动化结果不直接回填人工PASS。

### 2026-09-08 首次127.0.0.1运行：BLOCKED（历史）

- 用户已授权隔离依赖安装和临时实例测试。仅在 `.local/task34-browser-tools` 安装与CI一致的 `playwright@1.58.2`；使用 `--ignore-scripts` 和 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`，npm缓存也位于该目录。没有全局安装、下载浏览器或修改项目根依赖。
- Windows / 已安装 Chrome `152.0.7977.76`，`STEWARD_BROWSER_CHANNEL=chrome`；`NODE_PATH` 指向上述隔离 node_modules，`CARGO_TARGET_DIR` 指向 `.local/task34-target`。运行前 `cargo build --workspace --locked --offline` 通过。
- `smoke.cjs` 启动独立严格认证 taskd、新临时库/runtime和临时端口。Chrome访问脚本分配的 `http://127.0.0.1:<临时端口>` 后显示 **“基于安全策略，该网站不能访问！如有疑问请联系管理员。”**；没有出现工作台登录表单，等待登录输入框30秒超时，脚本exit 1。具体拦截层尚未定位，不归因为项目功能缺陷，也不绕过策略。
- 当时证据原为`.local/m5-failure.png`，后续重跑前已保留在 `.local/task34-screenshots-before-20260908-120125/m5-failure.png`（已核验截图内容；截图核验不是人工业务验收）。已有旧截图在执行前备份至 `.local/task34-screenshots-before-20260908-115031/`。项目专用截图没有生成，`project-smoke.cjs` 尚未进入。
- 脚本finally关闭浏览器并退出测试taskd；后续只读检查确认 `task34-target` 下运行的taskd数量为0，系统临时目录无剩余 `steward-gui-*`。测试未访问正式服务或默认库，Git HEAD/index均未改动。
- 本次taskd SHA-256：`793b12cfc1187acfdf95c84f0419363ba865f094592d838adf6b6b53be0a4af6`。HEAD仍为上文未提交现场；此hash仅绑定本次开发二进制，不是发布版本。
- 当时停止执行，等待用户提供允许的测试访问地址。随后用户明确指定本机 `http://172.19.10.185`，结果见下一节。未关闭安全软件或修改代理/浏览器安全策略，也未宣称127.0.0.1拦截原因已解决。

### 2026-09-08 用户指定地址运行：Windows浏览器自动化 PASS

- 地址：`http://172.19.10.185`。只读核验该IP属于本机、80端口未被占用后，脚本绑定该单一地址和80端口；若占用应失败，不接管已有服务。使用严格认证、新临时库/runtime/Git数据和隔离Chrome上下文，不访问正式库/服务。HTTP不加密，不作为网络部署安全保证。
- 修复默认80端口兼容：Origin使用浏览器规范形式（省略`:80`），Host仅接受绑定地址的等价默认端口写法；其它地址/端口、重复Host、非同源Origin、匿名访问与缺失CSRF仍拒绝。新增HTTP合同测试覆盖允许及拒绝分支。
- `smoke.cjs`增加`STEWARD_TEST_BIND`（默认127.0.0.1，必须为本机IPv4）、`STEWARD_TEST_PORT`（默认0）并校验实际启动地址；不支持对任意现有URL直接写入。Cookie测试使用实际hostname；普通HTTP的剪贴板测试走现有手动复制文本降级，不放宽浏览器安全限制。
- 第一次指定地址运行已进入工作台，原测试的`.task-panel`同时匹配隐藏项目面板而失败；修正为`#task-page .task-panel`后完整重跑exit 0。此前失败的`.local/m5-failure.png`可能仍在，不用残留失败图判断最终结果。
- 两段真实浏览器流程均通过：既有Task生命周期/并发/Worktree安全/浏览器授权/reader/SSE分页；新增Project创建/重名/独立CAS/组件/普通目录及Git源码/任务归属与组件/项目筛选/只读上下文/reader/原生必填与可选组件/桌面及390px布局。Source登记前后fixture字节/Git状态一致，关联不改变Task其它执行字段。覆盖范围以`smoke.cjs`和`project-smoke.cjs`断言为准，不等于全部人工用例或旧后端实浏览器测试已执行。
- 已查看 `.local/m5-projects-desktop.png`、`.local/m5-projects-mobile.png`、`.local/m5-mobile.png`；布局无水平溢出。截图中的`INSECURE_DATABASE_PERMISSIONS`是自定义临时库父目录未由taskctl管理的提示，未抑制或作为正式库权限合格证明；测试runtime/凭据另按原脚本校验保护ACL。
- 复核结束状态：隔离taskd数量0、80端口监听数量0、无`steward-gui-*`目录残留；未留下常驻实例。Git HEAD/index未变化，未提交/push、迁移或部署。
- 最终Windows回归：156项Rust、33项Node（23 DOM/VM、4 native、6 Pi，真实隔离Pi未跳过）通过；workspace Clippy `-D warnings`、build、fmt/diff检查通过；Rust LSP两文件零诊断。真实浏览器PASS另计，不混入Node单元测试数。
- 本次taskd SHA-256：`a9d6e3b82528d19216022333104255bf8dc0bac0cc14f4485a02b0f61116c998`。
- 项目桌面截图SHA-256：`85596337bd812251cf94110ca83e1ac7b09a24114910bc5c22dca9c3e63f252f`；项目390px截图：`799837ee9acb73b06353fb5de2d326f4ca4e838511d6abd8bb15378532579049`。

复现参数（仅在确认该地址为本机、端口可用且获得测试授权时执行；不是部署命令）：

```bash
NODE_PATH=E:/ai/agent-steward/.local/task34-browser-tools/node_modules \
CARGO_TARGET_DIR=E:/ai/agent-steward/.local/task34-target \
STEWARD_BROWSER_CHANNEL=chrome STEWARD_TEST_BIND=172.19.10.185 STEWARD_TEST_PORT=80 \
node crates/server/tests/browser/smoke.cjs
```

Linux、用户人工验收和正式Git历史整合仍未完成；后续迁移发布需另行设计与授权。

### 2026-09-08 Review 修复后回归：Windows 自动化 PASS

- R1：HTTP Git 查询仅选择登记 Repository 清单内的本机 checkout。覆盖普通/linked checkout 正例、未登记/不存在/父级跳转拒绝，以及 UNC/device/ADS 词法拒绝先于数据库打开；这些拒绝测试不执行网络探测。实际解析后仍检查身份。具体路径拼写和 CLI 差异见[第16篇](16-项目与上下文复用.md)。
- R2：覆盖 stdout/stderr 读取中限流、超时、取消、跨调用共享预算、已退出父进程的后代持有 pipe，以及 HTTP Future 取消后的 worker 并发槽恢复。Windows Job 终止是异步操作，测试要求后代在短等待内确认退出，而不是把发出终止请求当作已经退出。Unix 进程组/WNOWAIT 分支本机未执行；预算不保证挂起的原生文件 I/O 可硬取消。
- R3/R4：DOM/VM 和真实 Chrome 都覆盖“延迟组件响应 + 新编辑草稿”及“切换项目查询失败 + 恢复”；VM 还强制调用失败后的更多处理器，确认不会跨项目拼页。拦截仅作用于临时 Chrome 上下文的合成测试请求，不操作共享服务。
- 全量 **164 Rust / 35 Node** 测试通过（含真实 Pi 0.85.1，未跳过），workspace build、Clippy `-D warnings`、fmt/diff 检查通过；11 个修改 Rust 文件的 rust-analyzer 诊断为 0。
- 在原批准地址 `http://172.19.10.185` 重新执行完整 Chrome smoke，exit 0；桌面/390px截图已查看。旧截图先保留于 `.local/task34-screenshots-before-fix-20260908-144026/`。清理核验：隔离 taskd=0、80 端口监听=0、临时 GUI 目录=0，没有持续可用的测试地址。
- 本次 taskd SHA-256：`2df45d1c457c15102e3dba41c52b476bd8e71c3e06ab61b9191b2f3261b56c87`。桌面截图：`c303d1fcf4e8ff53b8b7d789f7253b2ad884d052db61af4b5a335e9ed5923ba4`；移动截图：`3a639fe04899972dc953f83816c33b4c0529d6785a8604e5e41f96566dc99603`。截图仍如实展示临时数据库权限告警，不作为正式部署权限验证。
- 没有提交、push、迁移正式库、发布或关闭任务。上述是修复后自动化证据，不回填下方人工 PASS；Linux/人工验收仍待安排。

### 2026-09-08 相邻问题修复后回归：Windows 自动化 PASS

- 旧 dialog `close` 事件：弹窗已重开时忽略旧关闭事件；DOM/VM 覆盖提交前、提交中到达及正常关闭清理。真实 Chrome 在同一浏览器任务内取消并重开，等待原生排队的 `close` 事件后成功创建项目，不再只依赖同步 DOM 桩。
- 共用 `run_read` 已覆盖 Task context/worktree-status、只读 doctor 和此前两个源码入口，普通写命令仍走原执行器。`crates/server/src/read_tests.rs` 通过 reader 的实际 Router 请求，让临时仓库的 Git clean filter 启动 30 秒合成等待，随后取消请求；三个旧入口均在短等待内释放共享槽，Task/History 和测试文件字节不变。只改临时仓库配置，不改进程全局 PATH/env 或真实仓库；没有对共享服务注入故障。
- 通用 Git 查询另覆盖作用域内超时及 stdout 流式限额。全量 **167 Rust / 37 Node** 通过（真实 Pi 未跳过），build、Clippy `-D warnings`、fmt/diff 检查通过；4 个变更 Rust 文件 rust-analyzer 零诊断。
- 在批准地址 `http://172.19.10.185` 完整 Chrome 两段流程再次 PASS，桌面/390px截图已查看。原截图保留于 `.local/task34-screenshots-before-close-fix-20260908-152522/`；结束后隔离 taskd、80 端口监听、GUI 临时目录、合成测试进程均为 0。
- 当前 taskd SHA-256：`7dd5d8dc7ff1baac350cb3d81b10e7d453ea7b696f3a301e37d3fdb133c4e94a`；项目桌面图：`5ba34f98c9be74fbef2e19fbd1d0e406a9b4979f86e0553df60139d2cecb25a0`；移动图：`d7d336923c1da3d99a4559d4e59c852f3d7eb19c8d7801e07574cfb94bc33fbd`。
- Linux/人工验收未执行，未提交、迁移、发布或关闭任务。预算仅控制 Git 和检查点，不是整个 HTTP/SQLite/原生文件 I/O 的硬时间保证；Task context/doctor 保留原有观测失败诊断合同，不把无法观察视作 clean。

### 2026-09-08 用户确认的本轮取舍

用户明确选择跳过 Linux 验证及人工验收，并授权进入本地 Git 整合与候选提交。跳过记为 SKIPPED，不是 PASS，不抹去未验证风险，也不删除 Linux 测试或调整 CI。上方各阶段记录保留为历史证据；本轮候选依赖已有 Windows 自动化及候选复测，不代表正式环境/业务验收通过。

候选包含项目管理主线、相关安全修复与测试文档，以及已实现但尚未接入生产自动注入链路的 Host/Pi 证据基础。缓存复用、Pi→Rust 桥接和成本节省仍不作为已完成能力。提交身份和最终复测结果以 #34 的后续 Checkpoint 为准；push、正式迁移、全局安装、发布和任务关闭不包含在本次 Git 授权内。

## 6. 回填与失败证据

以下为人工执行表；自动化结果仍见上方，不替代人工 PASS。

| 用例 | PASS / FAIL / BLOCKED / SKIPPED | 实际版本/环境 | 证据编号 | 备注 |
| --- | --- | --- | --- | --- |
| TC34-P1 | SKIPPED | 未执行 | #34 | 用户决定本轮跳过 |
| TC34-P2 | SKIPPED | 未执行 | #34 | 用户决定本轮跳过 |
| TC34-P3 | SKIPPED | 未执行 | #34 | 用户决定本轮跳过 |
| TC34-P4 | SKIPPED | 未执行 | #34 | 用户决定本轮跳过 |
| TC34-P5（分页/窄屏分开记录） | SKIPPED | 未执行 | #34 | 用户决定本轮跳过 |
| TC34-P6（reader/旧后端分开记录） | SKIPPED | 未执行 | #34 | 用户决定本轮跳过 |
| C-01 / CLI对照 | SKIPPED | 未执行 | #34 | 人工对照跳过，已有 CLI 自动化证据保留 |

失败最小回传：用例ID、执行时间、实际URL/版本与二进制hash、角色、数据代号和实际Project/Task/Source ID、期望与实际、最短步骤、脱敏截图、自然请求的方法/路径/状态/error.code/CAS字段；注明是否已写入以及受影响对象。禁止上传完整凭据、Cookie、私有会话或无限日志。

## 7. 后续门禁

Windows真实Chrome自动化已通过；Linux与人工验收按用户决定在本轮跳过，不用Windows/VM或截图核验替代。已获授权执行本地Git整合与候选提交；此后仍需单独设计并演练活动Schema2→4迁移、备份/停写/旧进程阻断、校验与回滚。push、安装扩展、正式迁移和部署仍另行确认，不因本文、提交或测试通过自动实施。
