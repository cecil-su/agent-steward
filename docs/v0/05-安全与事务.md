# 安全与事务

## 1. 初版信任模型

初版是本地单用户工具，由人决定任务领取、接管和 Worktree 使用范围。不建立 Owner、Registry、Approval 或多角色权限系统。

这不表示放弃基本数据和 Git 保护：CLI 仍需防止事务部分提交、并发覆盖、路径误用和误删 dirty Worktree。

## 2. SQLite 保护

- Task、Session、Checkpoint、Note 和 History 只通过 Application Service 写入；
- AI、Hook 和 GUI 不得直接执行数据库 mutation；
- 每个连接启用 `foreign_keys`、WAL 和有限 `busy_timeout`；
- 每次 mutation 使用短时 SQLite 写事务，并在事务内重新读取和校验 Task version；
- 业务变更和对应 History 必须同事务提交，任一步失败都整体回滚；
- 不另外维护跨进程锁文件，进程间写入由 SQLite 和乐观 version 共同协调；
- 不在数据库写事务中执行 Git 或 AI 客户端长操作；
- 数据库繁忙、version 不匹配或约束冲突时返回明确错误，不覆盖新版本；
- migration 必须在事务中按版本执行，数据库版本高于当前程序支持版本时拒绝写入；
- 数据库损坏或 migration 失败时不得自动删除、重建或覆盖用户数据库。

## 3. Claim 与接管

`task claim` 不是权限授权，只是记录当前处理该 Task 的 Session。

- 没有当前 Session 时直接领取；
- 相同 Session 重复领取时幂等成功；
- 已有其他当前 Session 时返回冲突和提示；
- 用户使用 `--take-over` 可以明确接管；
- 接管动作追加到 History，旧 Session 保留在历史中。

初版不使用自动超时或心跳回收；Session 接管只由用户明确触发。

## 4. Worktree 最低保护

默认禁止：

- `reset --hard`；
- `clean`；
- force checkout/switch；
- 隐式 stash；
- 未请求的 `git add -A`；
- Git push；
- dirty Worktree 强制删除。

`worktree remove` 必须先实时读取状态。存在 staged、unstaged 或 untracked 文件时拒绝删除，由用户先自行提交、复制、生成补丁或清理。

初版不持久化复杂的 Operation Plan。Git 操作与 SQLite 事务不能形成单一原子事务；如果 Git 已成功而数据库提交失败，命令必须报告真实现场、数据库引用状态和人工恢复方式，不能自动执行破坏性补偿，也不能伪装成完整成功。

## 5. Session 与隐私

- SQLite 数据库文件默认不提供内容级加密，因此 Task、Checkpoint 和导入内容不得保存 Token、Cookie、密码或授权请求头；
- 数据库放在当前用户的应用数据目录，不得主动放入共享目录或 Git 仓库；
- 初版优先保存 Session ID 和记录路径，不复制完整聊天；
- 用户明确导入 Session 文件时必须先提醒其检查敏感内容，导入副本保存在 SQLite 中；
- 不记录或推断模型隐藏 Chain of Thought；
- Session 摘要和 Checkpoint 不能自动改变 Task 状态；
- 后续 Hook / Runtime Adapter 必须在落盘前脱敏，只采集客户端实际可观察内容。

## 6. 必须保持的不变量

- Task ID 由数据库主键保证唯一；
- Task 的 `current_session_id` 必须引用已登记 Session；
- `resume` 后旧 Session 记录必须保留，并可通过 `continued_from` 和 History 查询；
- Checkpoint 必须关联 Task 和来源 Session；
- Task mutation 和对应 History 要么共同提交，要么共同回滚；
- Git 现场只能来自实时观察；
- dirty Worktree 不能由 `taskctl` 强制删除；
- AI 不能仅凭摘要、测试文本或 Session 结束自动关闭 Task。
