name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format:
    name: Rust formatting
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: npm
          cache-dependency-path: web/package-lock.json
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build and test readonly UI
        working-directory: web
        run: |
          npm ci --ignore-scripts --no-audit --no-fund
          npm test
          npm run build
          npm run sync:embedded
      - name: Build binaries
        run: cargo build --workspace --locked
      - name: Install pinned Pi 0.85.1 for mandatory real-host regressions (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $binary = & ./.github/scripts/install-test-pi.ps1 -Destination "$env:RUNNER_TEMP/steward-test-pi-0.85.1"
          "STEWARD_TEST_PI_BIN=$binary" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          "STEWARD_REQUIRE_REAL_PI=1" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
      - name: Native adapters and task-context regressions (real Pi required on Windows; unit-only on Linux)
        run: node --test integrations/tests/*.test.mjs crates/server/tests/browser/*.test.cjs
      - name: Clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - name: Rust tests
        run: cargo test --workspace --all-targets --locked
      - name: Windows launcher and update safety tests
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          ./distribution/windows/test.ps1 -Taskd target/debug/taskd.exe
          ./distribution/windows/test-local.ps1
      - name: Install browser test dependencies
        if: runner.os == 'Linux'
        run: |
          cd web
          npx --no-install playwright install --with-deps chromium
      - name: Browser smoke test
        if: runner.os == 'Linux'
        working-directory: web
        env:
          STEWARD_TEST_BIN_DIR: ${{ github.workspace }}/target/debug
          STEWARD_BROWSER_CHANNEL: chromium
        run: |
          STEWARD_TEST_UI_MODE=embedded npm run test:browser
          npm run test:browser
      - name: Upload browser screenshots
        if: always() && runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: browser-screenshots
          path: web/.artifacts/
          if-no-files-found: ignore
          retention-days: 7
